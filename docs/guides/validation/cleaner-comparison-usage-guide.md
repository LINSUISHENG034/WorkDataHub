# Cleaner Comparison Usage Guide

> Source of truth: `scripts/validation/CLI/cleaner_compare.py`
> Last verified: `2026-04-11`
> Scope: Validation tooling

This guide is the reference target for the cleaner comparison CLI.

## Purpose

Use `scripts/validation/CLI/cleaner_compare.py` to compare legacy and current cleaner outputs when validating migration behavior or investigating parity gaps.

## Basic Usage

```bash
uv run python scripts/validation/CLI/cleaner_compare.py --help
```
