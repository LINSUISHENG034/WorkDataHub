# 部署验证问题记录与后续验证计划（2026-03-08）

## 1. 背景

本次工作目标是验证 `docs/deployment_run_guide.md` 在当前环境下的可执行性，并重点核对以下两类能力：

1. Seed 数据写入是否完整，尤其是 `config/seeds/002` 中的大体量 CSV。
2. ETL 中的 EQC 查询是否能够真实执行，并确认内网环境下“查询数量受限制”的根因。

由于当前工作目录和本机 PostgreSQL 已存在基准环境，本次已优先采用**临时数据库隔离验证**方式，避免对现有数据库造成影响。

---

## 2. 已完成的临时隔离验证

### 2.1 验证方式

已使用临时数据库进行验证，数据库名称均采用 `wdh_test_*` 前缀，验证结束后已全部删除：

- `wdh_test_seed_28fffe9d`
- `wdh_test_eqc_9b12cbca`
- `wdh_test_etl_efb1ee57`

验证过程中未对现有常驻数据库对象做保留写入。

### 2.2 Seed 验证结果

对 `config/seeds/002` 中以下 3 个文件进行了实际落库验证：

- `config/seeds/002/客户明细.csv`
- `config/seeds/002/base_info.csv`
- `config/seeds/002/enrichment_index.csv`

CSV 统计与数据库实际写入结果一致：

| 对象 | CSV 行数 | 实际落库行数 | 结果 |
|---|---:|---:|---|
| `customer."客户明细"` | 10,306 | 10,306 | ✅ 一致 |
| `enterprise.base_info` | 27,689 | 27,689 | ✅ 一致 |
| `enterprise.enrichment_index` | 45,105 | 45,105 | ✅ 一致 |

结论：**当前代码路径下，`config/seeds/002` 可以完整写入，不存在本次复现中的“无法全部写入”问题。**

### 2.3 EQC 验证结果

在隔离库中进行 ETL + EQC 验证时，发现当前环境中的 EQC Token 处于**存在但无效/已过期**状态：

- Token 存在：是
- Token 校验结果：无效/已过期
- 当前限流配置：`30 次 / 20 秒`
- 配置层默认同步预算：`5`
- CLI 默认同步预算参数：`500`

结论：**当前环境尚不能证明 EQC 真实外呼有效执行，因为首先被 Token 有效性问题阻断。**

### 2.4 ETL 样本落库问题

使用样本文件进行单领域 ETL 验证时，又发现一个与 EQC 无关的独立问题：

- 在 `--no-enrichment` 条件下，`annuity_performance` 的 ETL 仍然返回成功退出码。
- 但业务事实表 `business."规模明细"` 最终写入行为为 `0` 行。

这说明当前样本验证路径中，除了 EQC Token 失效外，还存在**ETL 样本文件/参数/执行链路未真正落表**的问题，需要单独排查。

---

## 3. 当前发现的问题清单

### P1. `root` 账号无法连接目标数据库

原计划目标数据库为：

- `postgresql://root:Post.169828@192.168.0.200:5432/postgres`

但当前实际验证中，`root` 账号连接失败，表现为：

- PostgreSQL 用户 `root` 认证失败。

这意味着后续若要切换到该数据库执行“保留落库不删除”的复核验证，**必须先完成 root 账号可用性修复**。

### P2. EQC Token 当前无效/已过期

当前环境下，ETL 预检查已能明确输出 Token 无效/过期状态，因此后续完整 EQC 验证不能直接开始。

### P3. ETL 样本文件验证链路存在“成功退出但未落表”问题

即便关闭 EQC，当前用于验证的样本命令也未写入 `business."规模明细"`。这会影响后续对 EQC 命中数、缓存回写数、落库结果的判断。

### P4. 部署指南存在 CLI 参数示例偏差

`docs/deployment_run_guide.md` 中多处使用 `--domain` 示例，但当前 CLI 实际参数为 `--domains`。在继续正式验证前，需要把该偏差纳入复核范围。

---

## 4. 后续验证目标（保留落库复核模式）

后续验证不再使用“验证后删除”的临时数据库，而是改为使用以下目标数据库进行**完整落库且不删除**验证，便于后续 SQL 复核与人工抽查：

- `postgresql://root:Post.169828@192.168.0.200:5432/postgres`

> 说明：后续执行时建议统一显式使用 `postgresql://` 形式。

后续完整验证的交付目标为：

1. Seed 数据完整写入并保留结果。
2. ETL 全流程至少完成一次真实业务落库并保留结果。
3. EQC 至少完成一次真实可观察外呼、命中、缓存回写。
4. 最终输出可复核 SQL、表计数、日志摘要、问题定位结论。

---

## 5. 分阶段验证计划

### 阶段 0：前置阻塞解除（必须先完成）

本阶段未完成前，不进入“保留落库不删除验证”。

#### 0.1 修复 `root` 账号连接问题

目标：确保以下连接串可成功连接并具备建表、迁移、写入权限：

- `postgresql://root:Post.169828@192.168.0.200:5432/postgres`

验收标准：

- 可成功 `SELECT current_user, current_database()`。
- 可成功执行 Alembic 迁移到 `head`。
- 可成功对目标 schema 执行插入、更新、查询。

#### 0.2 刷新/修复 EQC Token

目标：确保当前环境中的 EQC Token 可通过预校验。

验收标准：

- ETL 启动时输出 `Token valid`。
- 手动 EQC 校验命令返回成功。

#### 0.3 先修复 ETL 样本“0 行落库”问题

目标：先在不依赖 EQC 的情况下，让 `annuity_performance` 样本 ETL 至少能真实写入业务事实表。

验收标准：

- `business."规模明细"` 至少写入 1 行以上。
- CLI Summary 中 `Inserted` 大于 0。

### 阶段 1：在目标数据库上执行 Seed 保留验证

完成阶段 0 后，直接在以下数据库执行：

- `postgresql://root:Post.169828@192.168.0.200:5432/postgres`

执行目标：

1. 执行 Alembic 迁移到 `20251228_000003` 或 `head`（以最终验证策略为准）。
2. 确认 Seed 自动加载已执行。
3. 对以下对象做行数核对并保留结果：
   - `customer."客户明细"`
   - `enterprise.base_info`
   - `enterprise.enrichment_index`
4. 将行数、唯一键计数、抽样查询结果记录到验证文档中。

验收标准：

- 3 张重点 Seed 表行数与 `config/seeds/002` 对应 CSV 一致。
- 无大字段 CSV 解析失败、JSONB 解析失败、唯一键冲突导致的异常中断。

### 阶段 2：在目标数据库上执行 ETL 实际落库验证

执行目标：

1. 使用样本文件先做最小化真实写入验证。
2. 确认 `business."规模明细"` 有实际写入。
3. 如最小化验证通过，再扩展到更完整样本或真实账期目录。

优先验证对象：

- `business."规模明细"`
- 如需要联动验证，再检查：
  - `customer."客户年金计划"`
  - `customer."客户业务月度快照"`
  - `customer."客户计划月度快照"`

验收标准：

- ETL 命令真实写入，`Inserted > 0`。
- 表中存在可查询的业务事实数据。
- 若关闭 post-hooks，则仅验证事实表。
- 若开启 post-hooks，则同步验证合约状态和快照表。

### 阶段 3：在目标数据库上执行 EQC 真实外呼验证

执行目标：

1. 开启 enrichment，执行带同步预算的 ETL。
2. 确认至少发生一次真实 EQC 命中或真实 EQC 外呼。
3. 核对以下结果是否落地：
   - `enterprise.base_info`
   - `enterprise.enrichment_index`
4. 核对日志统计字段：
   - `eqc_hits`
   - `budget_consumed`
   - `budget_remaining`
   - `db_cache_hits`
   - `yaml_hits`

验收标准：

- 日志中出现可复核的 EQC 执行痕迹。
- `enterprise.base_info` / `enterprise.enrichment_index` 至少一张表有新增数据。
- 能区分“缓存命中”与“真实外呼命中”。

### 阶段 4：结果保留与复核

本阶段不删除目标数据库中的验证结果，用于后续人工复核。

需保留内容：

- 数据库中的最终落表结果。
- 验证命令清单。
- 关键表行数统计。
- 日志摘录。
- 验证结论与未解决问题。

建议复核维度：

1. Seed 行数对账。
2. ETL 事实表落库结果。
3. EQC 外呼与缓存写入结果。
4. 指南命令与实际 CLI 行为一致性。

---

## 6. 当前建议的执行顺序

1. 先修复 `root` 认证失败。
2. 再修复/刷新 EQC Token。
3. 单独定位 `ETL 成功退出但 0 行落库` 的问题。
4. 完成以上 3 项后，切换到 `postgresql://root:Post.169828@192.168.0.200:5432/postgres` 做不删除的正式验证。
5. 最后将正式验证结果回填到本计划文档或配套结果文档中。

---

## 7. 当前状态结论

截至 2026-03-08，本次验证结论如下：

- `config/seeds/002` 完整写入：**已通过临时隔离验证**。
- EQC 真实执行验证：**被无效/过期 Token 阻断，未完成**。
- ETL 实际业务落库验证：**发现 0 行写入问题，未完成**。
- 使用 `root` 账号在目标数据库 `postgresql://root:Post.169828@192.168.0.200:5432/postgres` 上做“保留落库不删除验证”：**尚未具备执行条件**。

后续工作应以“解除阶段 0 阻塞项”为第一优先级。
